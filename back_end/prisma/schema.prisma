// ======================================================
// COMPONENTE: Prisma Schema (User Domain)
// ======================================================
//
// ARQUIVO:
// prisma/schema.prisma
//
// DESCRIÇÃO:
// Arquivo responsável por definir a estrutura do banco
// de dados da aplicação utilizando o Prisma ORM.
//
// FUNÇÃO:
// - Declarar o gerador do Prisma Client
// - Configurar a fonte de dados (PostgreSQL)
// - Definir os modelos e relações do domínio
//
// OBJETIVOS:
// - Garantir consistência entre código e banco de dados
// - Centralizar a definição do schema
// - Facilitar migrações e versionamento do banco
// - Suportar recursos de autenticação e recuperação de senha
// ======================================================

// ===============================================
// 🌐 CONFIGURAÇÃO DO CLIENT PRISMA
// ===============================================
generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

// ===============================================
// 🗄️ CONFIGURAÇÃO DO BANCO DE DADOS
// ===============================================
datasource db {
  provider = "postgresql"
}

// ===============================================
// 👤 TABELA: USER
// ===============================================
model User {
  id       String  @id @default(uuid())
  email    String  @unique
  name     String?
  password String
  // Adicione estes campos:
  resetToken       String?
  resetTokenExpiry DateTime?
  cep      String
}

/*
backend/
├── src/
│   ├── controllers/
│   │   └── password.controller.ts ✅ (já criado no artifact)
│   ├── routes/
│   │   └── password.routes.ts
│   ├── lib/
│   │   └── mail.ts
│   └── server.ts (atualizar)
├── prisma/
│   └── schema.prisma (adicionar campos)
└── .env (adicionar variáveis de email)

*/
